language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t vabhs192003/docker-react -f Dockerfile.dev .

script: 
  - docker run -e CI=true vabhs192003/docker-react npm run test